#include <helper.h>
#include <page_directory.h>
#include <types.h>

void * page_directory_get(uintptr_t index)
{
  MARK_PARAM_USED(index);
  return NULL;
}

void * page_directory_add(uintptr_t index)
{
  MARK_PARAM_USED(index);
  return NULL;
}
